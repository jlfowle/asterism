apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: unifi-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-secretsmanager
  target:
    name: unifi-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: /asterism/unifi
