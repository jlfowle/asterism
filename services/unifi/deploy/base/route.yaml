apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: unifi
spec:
  to:
    kind: Service
    name: unifi
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
