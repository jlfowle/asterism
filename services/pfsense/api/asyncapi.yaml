asyncapi: 3.0.0
info:
  title: pfsense events API
  version: 0.1.0
channels:
  pfsense.status.updated:
    address: pfsense.status.updated
    messages:
      statusUpdated:
        $ref: '#/components/messages/StatusUpdated'
operations:
  publishStatusUpdated:
    action: send
    channel:
      $ref: '#/channels/pfsense.status.updated'
    messages:
      - $ref: '#/channels/pfsense.status.updated/messages/statusUpdated'
components:
  messages:
    StatusUpdated:
      payload:
        type: object
        properties:
          type:
            type: string
          source:
            type: string
          timestamp:
            type: string
            format: date-time
          payload:
            type: object
            additionalProperties: true
