apiVersion: v1
kind: Service
metadata:
  name: polaris
spec:
  selector:
    app.kubernetes.io/component: shell
    app.kubernetes.io/name: polaris
    app.kubernetes.io/part-of: asterism
  ports:
    - name: http
      port: 80
      targetPort: http
